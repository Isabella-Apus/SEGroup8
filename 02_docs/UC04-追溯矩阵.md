# UC04 追溯矩阵

> Day 3 更新。用户列表、封禁、解禁和审计查询的 API 契约已登记；封禁后的真实登录联动属于后续回归。

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 API/UNIT 测试 | 后续 E2E | 状态 |
|---|---|---|---|---|---|
| AC04-01 封禁后 BANNED 且登录失败 | `UC04-COMP04` 封禁/登录分支 | `AdminUserController`、`AdminUserServiceImpl.banUser`、`AuthServiceImpl.login`、`user.status` | `INT04-01`、`UNIT04-01`、`AuthServiceImplTest.login_shouldRejectBannedUser` | `E2E04-01` 待 Day 4 | 封禁和登录拒绝规则已覆盖；完整链待回归 |
| AC04-02 解禁后 NORMAL 且可登录 | `UC04-COMP04` 解禁分支 | `AdminUserController`、`AdminUserServiceImpl.unbanUser`、`user.status` | `INT04-02`、`UNIT04-02` | `E2E04-02` 待 Day 4 | 解禁 API/Service 已覆盖；登录联动待回归 |
| AC04-03 非 ADMIN/不能自封禁 | `AdminUserServiceImpl` 权限分支 | `UserContext`、`user.role` | `UNIT04-03/04`、`INT05-11` | `E2E04-03` 待 Day 4 | 非管理员和自封禁规则已覆盖 |
| AC04-04 审计完整可查 | `UC04-COMP04` Audit 分支 | `AdminAuditLogService`、`AdminAuditLogController`、`admin_audit_log` | `INT04-04`、`UNIT04-05`、封禁/解禁 Controller 审计调用测试 | `E2E04-04` 待 Day 4 | API 查询和写入契约已覆盖；真实数据待回归 |
| AC04-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` | 本矩阵、API 测试计划、测试类路径 | — | 已建立 |

## 数据归属

- `user` 和 `admin_audit_log` 由 `identity-governance-service` 负责。
- 认证服务只读取身份治理服务提供的用户状态/身份契约；微服务拆分后不直接共享 `UserMapper`。
