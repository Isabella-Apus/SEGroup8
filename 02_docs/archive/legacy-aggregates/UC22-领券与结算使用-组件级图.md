# UC22 领券及结算使用：组件级图

图号：`COMP-UC22-01`  
需求：`REQ22`  
用例：`UC22`

```mermaid
flowchart LR
  subgraph Browser[前端]
    Coupon[CouponCenterView.vue]
    Checkout[ProductDetailView.vue / 结算页]
    Api[voucher.js / order.js]
  end

  subgraph Backend[后端应用]
    VC[VoucherController]
    OC[OrderController]
    VS[VoucherService]
    OS[OrderServiceImpl]
    Tx[Spring 事务边界]
    VM[VoucherMapper]
    UVM[UserVoucherMapper]
  end

  Voucher[(voucher)]
  UserVoucher[(user_voucher)]
  Order[(order_info)]

  Coupon --> Api --> VC --> VS
  Checkout --> Api --> OC --> OS
  OS -->|shopAmounts + totalAmount| VS
  VS -->|校验状态/店铺/门槛/占用| Tx
  Tx --> VM --> Voucher
  Tx --> UVM --> UserVoucher
  Tx --> Order
  Tx -.下单失败回滚/释放.-> UserVoucher
```

## 关键规则

- 店铺券以匹配店铺的小计作为门槛金额，不得使用整单其他店铺金额凑门槛。
- `eligibleAmount < minAmount` 时必须在占用用户券前失败。
- 用户券占用使用条件更新，避免并发订单重复占用。
- 下单事务失败时不遗留 `used_order_id`。

## 对应测试

- `UNIT-UC22-001`：店铺小计未达到门槛时拒绝核销。
- `UNIT-UC22-002`：门槛失败不更新 `user_voucher`。
