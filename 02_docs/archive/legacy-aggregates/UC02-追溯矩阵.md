# UC02 追溯矩阵

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 自动化测试 | 状态 |
|---|---|---|---|---|
| AC02-01 资料查询/修改本人 | `UC02-COMP02` 资料分支 | `UserController`、`UserServiceImpl`、`user` | `UserControllerWebMvcTest.profile_shouldReturnCurrentUser`、既有资料映射测试 | API + Service 已覆盖 |
| AC02-02 地址新增、查询、修改 | `UC02-COMP02` 地址写入分支 | `UserController`、`UserServiceImpl`、`AddressMapper`、`address` | `UserControllerWebMvcTest.createAddress_shouldReturnSuccess`、`deleteAddress_shouldReturnSuccess` | API 主路径已覆盖；查询/修改回归待补 |
| AC02-03 默认地址唯一 | `UC02-COMP02` 默认地址分支 | `UserServiceImpl.createAddress`、`address.is_default` | 既有 `UserServiceImplTest.createAddress_whenDefault_shouldClearPreviousDefault` | Service 已覆盖；API 回归待补 |
| AC02-04 越权操作和删除后不可见 | `UC02-COMP02` 归属判断分支 | `UserServiceImpl.updateAddress/deleteAddress` | `UserControllerWebMvcTest.createAddress_shouldRejectInvalidPhone`、既有他人地址删除测试 | 参数/API + Service 异常已覆盖；多用户 E2E 待补 |
| AC02-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` 的 `user/address` | 测试类和 Maven 命令见 API 测试计划 | 已建立 |

## 数据归属

- `user`、`address` 均由 `identity-governance-service` 负责。
- 其他服务只通过用户摘要或内部契约获取必要身份信息，不直接读取 A 域 Mapper。

