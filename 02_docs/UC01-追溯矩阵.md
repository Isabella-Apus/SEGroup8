# UC01 追溯矩阵

> Day 3 更新。API/拦截器/JWT 契约已登记；真实多角色数据库链和前端 E2E 属于后续回归。

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 API/UNIT 测试 | 后续 E2E | 状态 |
|---|---|---|---|---|---|
| AC01-01 注册成功、密码不明文 | `UC01-COMP01` 注册分支 | `AuthController.register`、`AuthServiceImpl.register`、`user` | `INT01-01/04`、`UNIT01-01/03` | `E2E01-01` 待 Day 4 | API、重复用户名和密码加密已覆盖 |
| AC01-02 登录返回 JWT、角色、摘要 | `UC01-COMP01` 登录分支 | `AuthController.login`、`AuthServiceImpl.login`、`JwtUtils` | `INT01-02/04`、`UNIT01-02/04`、`JwtUtilsTest` | `E2E01-02` 待 Day 4 | 登录 API、封禁拒绝和 JWT 生成已覆盖 |
| AC01-03 参数、重复用户名、错误密码失败 | `AuthController` 校验与 `AuthServiceImpl` 异常 | `RegisterRequest`、`LoginRequest`、`AuthServiceImpl` | `INT01-03/04`、`UNIT01-03`、错误密码 Service 测试 | `E2E01-03` 待 Day 4 | API 与 Service 已覆盖 |
| AC01-04 USER/ADMIN 角色边界与 Token 校验 | `JwtAuthInterceptor` + `AccessControl` | `WebMvcConfig`、`JwtUtils`、`security-contract` | `JwtAuthInterceptorTest`、`JwtUtilsTest`、`JwtTokenVerifierTest`、`INT05-11` | `E2E01-03` 待 Day 4 | JWT/filter 契约和非管理员拒绝已覆盖；跨服务业务接入待迁移 |
| AC01-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` 接口/表归属设计 | 本矩阵、API 测试计划、测试类路径 | — | 已建立 |

## 数据归属

- `user` 表由 `identity-governance-service` 负责。
- JWT 是跨服务传递的身份凭证，不作为其他服务的用户数据表替代品。
- 角色最终授权以服务端用户状态/角色校验为准。
