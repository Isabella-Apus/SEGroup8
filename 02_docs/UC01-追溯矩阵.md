# UC01 追溯矩阵

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 自动化测试 | 状态 |
|---|---|---|---|---|
| AC01-01 注册成功、密码不明文 | `UC01-COMP01` 注册分支 | `AuthController.register`、`AuthServiceImpl.register`、`user` | `AuthControllerWebMvcTest.register_shouldReturnUnifiedSuccess`、既有 `AuthServiceImplTest.register_shouldEncodePasswordAndInsertUser` | 已覆盖 Controller + Service |
| AC01-02 登录返回 JWT、角色、摘要 | `UC01-COMP01` 登录分支 | `AuthController.login`、`AuthServiceImpl.login`、`JwtUtils` | `AuthControllerWebMvcTest.login_shouldReturnTokenAndRole`、`JwtUtilsTest.createAndParse_shouldPreserveClaims` | 已覆盖 API 契约 + JWT |
| AC01-03 参数、重复用户名、错误密码失败 | `AuthController` 校验与 `AuthServiceImpl` 异常 | `RegisterRequest`、`LoginRequest`、`AuthServiceImpl` | `AuthControllerWebMvcTest.register_shouldRejectInvalidBody`、既有错误密码测试 | 参数/API 已覆盖；重复用户名服务测试已有缺口 |
| AC01-04 USER/ADMIN 角色边界 | `JwtAuthInterceptor` + `AccessControl` | `WebMvcConfig`、`AdminUserController`、`user.role` | `JwtUtilsTest.parse_shouldRejectTamperedToken`；完整权限链列入 API 回归 | JWT 已覆盖；跨 Controller 链待后续 |
| AC01-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` | 测试类和 Maven 命令见 API 测试计划 | 已建立 |

## 数据归属

- `user` 表由 `identity-governance-service` 负责。
- JWT 是跨服务传递的身份凭证，不作为其他服务的用户数据表替代品。
- 角色最终授权以服务端用户状态/角色校验为准。

