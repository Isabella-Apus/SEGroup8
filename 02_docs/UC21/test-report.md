# UC21 测试报告

执行时间：2026-08-28 16:39（Asia/Shanghai）

后端结果：7 个 UC21 相关测试通过，0 失败，0 跳过。最新主线的完整 `DOMAIN_E` 入口共 19 个测试通过。

浏览器结果：真实 Compose 栈上 1 个 Playwright 用例通过，0 失败，0 跳过，耗时 7.5 秒。浏览器访问 Nginx 前端 `http://127.0.0.1:8088`，前端经 `/api` 调用 Spring Boot，健康端点确认后端连接 MySQL。

已验证：

- 卖家店铺券创建、查询、修改、关闭和删除；
- 管理员创建平台券，平台券不绑定店铺；
- 普通用户访问管理员入口返回业务码 403；
- 其他卖家不能修改不属于自己的券；非法类型、金额、门槛、时间和总量不写库；
- 已使用优惠券不能删除，关闭后不能继续领取；
- 关闭、删除结果写入测试数据库。
- 卖家在页面完成创建、编辑、关闭；刷新后名称和关闭状态保持不变；
- 普通买家调用管理员创建入口返回业务码 403。

整体状态：`PASS`。原始 JUnit、Playwright JSON/XML/HTML、截图和健康检查均保存在 `04_tests/UC21/evidence/`。
