# UC02 追溯矩阵

> Day 3 更新。Controller API 和 Service 规则测试已登记；真实多用户数据库隔离和前端 E2E 属于后续回归。

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 API/UNIT 测试 | 后续 E2E | 状态 |
|---|---|---|---|---|---|
| AC02-01 资料查询/修改本人 | `UC02-COMP02` 资料分支 | `UserController`、`UserServiceImpl`、`user` | `INT02-01/02/03`、`UNIT02-01` | `E2E02-01` 待 Day 4 | 查询/修改 API 已覆盖；真实持久化待回归 |
| AC02-02 地址新增、查询、修改 | `UC02-COMP02` 地址写入分支 | `UserController`、`UserServiceImpl`、`AddressMapper`、`address` | `INT02-04/06/07`、`UNIT02-02` | `E2E02-02` 待 Day 4 | API 与默认地址规则已覆盖 |
| AC02-03 默认地址唯一 | `UC02-COMP02` 默认地址分支 | `UserServiceImpl.createAddress`、`address.is_default` | `INT02-04`、`UNIT02-02` | `E2E02-02` 待 Day 4 | Service 规则和新增 API 已覆盖 |
| AC02-04 越权操作和删除后不可见 | `UC02-COMP02` 归属判断分支 | `UserServiceImpl.updateAddress/deleteAddress` | `INT02-07/08`、`UNIT02-03` | `E2E02-03` 待 Day 4 | API 路由和 Service 归属异常已覆盖；真实用户隔离待回归 |
| AC02-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` 的 `user/address` | 本矩阵、API 测试计划、测试类路径 | — | 已建立 |

## 数据归属

- `user`、`address` 均由 `identity-governance-service` 负责。
- 其他服务只通过用户摘要或内部契约获取必要身份信息，不直接读取 A 域 Mapper。
