# UC22 测试报告

执行时间：2026-08-28 17:21–17:25（Asia/Shanghai）。

## 结果

| 检查 | 结果 |
|---|---|
| UC22 后端标签 | 7 通过，0 失败，0 跳过 |
| 完整 `DOMAIN_E` 回归 | 23 通过，0 失败，0 跳过 |
| 前端生产构建 | PASS，Vite 转换 2421 个模块 |
| UC22 Playwright | 1 通过，0 失败，0 跳过；用例 5.3 秒，总耗时 6.7 秒 |
| Compose 健康 | frontend、backend、database 均为 healthy |

## 浏览器链路

Playwright 访问 Nginx 前端 `http://127.0.0.1:8088`。卖家通过真实接口创建一次性店铺券，买家在页面点击“立即领取”，随后在商品详情选择该券并确认地址。订单接口返回商品金额 299.00、优惠金额 10.00、应付金额 289.00。买家在订单详情完成模拟微信支付，页面显示待发货。刷新领券中心后，“已使用”页签仍显示该券。

测试准备代码只创建缺失的默认地址和一次性优惠券。领券、选券、下单、支付与刷新均由浏览器操作完成。

## 后端边界

JUnit 验证了重复领取拒绝、不可领取状态、领完拒绝、店铺门槛、失败不占用、取消释放与支付核销。完整 Domain-E 回归同时覆盖 UC21 与 UC23–UC25 的共享服务测试。

整体状态：`PASS`。Git 保留 Surefire XML、结构化结果和关键截图；完整 Playwright HTML、trace、video 由 Actions artifact 保存。当前 `main@b622e6bb` 的完整系统流水线 33526387696 已确认 UC01-UC25 全部具备独立 spec 并执行通过。
