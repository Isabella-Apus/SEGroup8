# UC04 追溯矩阵

| 验收条件 | 组件级设计 | 代码/数据 | Day 3 自动化测试 | 状态 |
|---|---|---|---|---|
| AC04-01 封禁后 BANNED 且登录失败 | `UC04-COMP04` 封禁/登录分支 | `AdminUserServiceImpl.banUser`、`AuthServiceImpl.login`、`user.status` | `AdminUserControllerWebMvcTest.ban_shouldReturnSuccessAndRecordAudit`、既有封禁 Service 测试 | 封禁 API/Service 已覆盖；登录联动待回归 |
| AC04-02 解禁后 NORMAL 且可登录 | `UC04-COMP04` 解禁分支 | `AdminUserServiceImpl.unbanUser`、`user.status` | `AdminUserServiceImplTest.unbanUser_shouldRestoreNormalStatus`、Controller 解禁测试 | 已覆盖状态/API |
| AC04-03 非 ADMIN/不能自封禁 | `AdminUserServiceImpl` 权限分支 | `UserContext`、`user.role` | `AdminUserServiceImplTest.banUser_shouldRejectSelfBan`、既有非 ADMIN 测试 | Service 已覆盖；完整拦截器链待回归 |
| AC04-04 审计完整可查 | `UC04-COMP04` Audit 分支 | `AdminAuditLogService`、`AdminAuditLogController`、`admin_audit_log` | Controller 验证 `record` 调用、既有审计写入测试 | 写入已覆盖；查询 API 待回归 |
| AC04-05 可双向追溯 | 本文件与 Day2 总矩阵 | `identity-governance-service` | 测试类和 Maven 命令见 API 测试计划 | 已建立 |

## 数据归属

- `user` 和 `admin_audit_log` 由 `identity-governance-service` 负责。
- 认证服务只读取身份治理服务提供的用户状态/身份契约；微服务拆分后不直接共享 `UserMapper`。

