# UC02 用户资料与地址设计

本文件沿用 UC06 的独立用例设计结构，仅覆盖 Day 3 组件级设计；对象级图和 E2E 留到 Day 4。

## 接口设计

| 方法 | 路径 | 控制器 | 服务接口/实现 | 数据归属 |
|---|---|---|---|---|
| GET | `/api/user/profile`、`/api/user/me` | `UserController` | `UserService.getCurrentUserProfile` | `user` |
| PUT | `/api/user/profile` | `UserController` | `UserService.updateCurrentUserProfile` | `user` |
| GET | `/api/user/addresses` | `UserController` | `UserService.listMyAddresses` | `address` |
| POST | `/api/user/addresses` | `UserController` | `UserService.createAddress` | `address` |
| PUT | `/api/user/addresses/{addressId}` | `UserController` | `UserService.updateAddress` | `address` |
| DELETE | `/api/user/addresses/{addressId}` | `UserController` | `UserService.deleteAddress` | `address` |

## 系统级交互

~~~mermaid
sequenceDiagram
    autonumber
    actor User as 已登录用户
    participant Platform as 交易平台
    User->>Platform: GET /api/user/profile
    Platform-->>User: 返回本人资料
    User->>Platform: PUT /api/user/profile
    Platform-->>User: 返回保存结果
    User->>Platform: POST /api/user/addresses
    Platform-->>User: 地址新增成功
    User->>Platform: GET /api/user/addresses
    Platform-->>User: 返回本人地址列表
    User->>Platform: PUT/DELETE /api/user/addresses/{addressId}
    Platform-->>User: 返回修改/删除结果
~~~

## 组件级交互

~~~mermaid
sequenceDiagram
  actor User as 登录用户
  participant Guard as JwtAuthInterceptor
  participant Context as UserContext
  participant API as UserController
  participant Svc as UserServiceImpl
  participant U as UserMapper
  participant A as AddressMapper
  participant DB as user/address

  User->>Guard: Bearer JWT 请求资料/地址
  Guard->>Context: setUserId(uid)
  Guard->>API: 放行

  User->>API: GET /api/user/profile
  API->>Svc: getCurrentUserProfile()
  Svc->>Context: getUserId()
  Svc->>U: selectById(uid)
  U->>DB: SELECT user
  DB-->>U: 当前用户
  Svc-->>API: UserVO
  API-->>User: Result<UserVO>

  User->>API: POST /api/user/addresses
  API->>Svc: createAddress(request)
  Svc->>Context: getUserId()
  alt isDefault=1
    Svc->>A: 清除当前用户其他默认地址
    A->>DB: UPDATE address SET is_default=0
  end
  Svc->>A: insert(address.userId=uid)
  A->>DB: INSERT address
  API-->>User: Result.success()

  User->>API: DELETE /api/user/addresses/{id}
  API->>Svc: deleteAddress(id)
  Svc->>A: selectById(id)
  alt 地址属于当前用户
    Svc->>A: deleteById(id)
    A->>DB: DELETE address
    API-->>User: success
  else 地址不属于当前用户
    API-->>User: 业务错误
  end
~~~


核心边界为 `UserController → UserServiceImpl → UserMapper/AddressMapper`。当前用户 ID 由 `JwtAuthInterceptor` 写入 `UserContext`，Service 层每次按当前用户 ID 限定查询和修改范围。

## 组件职责与规则

- `UserController`：路由、请求体校验和统一响应。
- `UserServiceImpl`：资料映射、地址归属校验、默认地址清理和删除。
- `UserMapper`：归属 `user` 表；`AddressMapper`：归属 `address` 表。
- 地址新增/修改涉及默认地址时，先清除当前用户其他默认地址，再写入目标地址。
- 修改和删除必须先按地址 ID 查询，再比较 `address.user_id` 与当前用户 ID。
- 未登录由 JWT 拦截器阻断；业务层仍保留 `UserContext` 空值保护。

## Day 3 测试边界

- 已补充 UserController API 契约测试：资料返回、地址新增、非法手机号和地址删除。
- 已保留既有 Service 测试：资料映射、默认地址清理、删除他人地址失败。
- 多用户真实数据库隔离和前端 E2E 留到后续回归。
