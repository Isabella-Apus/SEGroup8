# 全局工作规则

## 默认不新增替代性防线

- 默认不新增内容 hash、冻结 contract、baseline 或 gate。
- 只有在能够明确写出一个具体失败场景，并逐项说明 Git、版本号、主键、事务、唯一约束、类型和普通测试为何都不足以预防或发现该失败时，才允许新增上述机制。
- 不得以简化实现为理由删除已有安全措施。
- gate 只允许设置在不可逆操作、跨系统交互、安全边界或正式发布边界。
- 前置检查不得挤占或替代真正的代码执行、模拟或测量；验证必须保留对实际行为的执行与观测。
