# UC22 Evidence

测试完成后，本目录保存以下材料：

- `raw-reports/TEST-*.xml`：UC22 与 Domain-E Surefire 原始结果。
- `raw-reports/playwright/`：Playwright JSON、XML 和 HTML 报告。
- `screenshots/uc22-voucher-used-after-paid-order-refresh.png`：支付后刷新“已使用”页签。
- `logs/runtime-health.txt`：Compose 服务与 HTTP 健康检查。

本目录不保存密码、token、私钥或生产 Secret。
