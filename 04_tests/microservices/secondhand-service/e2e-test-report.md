# secondhand-service E2E 测试报告

同一 run 的 `UC16-UC19 independent service API E2E` 和 `UC16-UC20 Compose Playwright regression` jobs 均成功。共享 [Kinda Goods CI/CD](https://github.com/Isabella-Apus/SEGroup8/actions/runs/33526387696) 同 SHA 的完整回归也成功。完整 HTML、trace、video、重复截图和 Compose 日志只保存在 Actions artifact。
