# UC06-UC10 自动化测试报告

## 执行摘要

| 项目 | 结果 |
|---|---|
| 执行日期 | 2026-08-25 |
| 环境 | Windows 11、JDK 17.0.13、Maven 3.9.14、Spring Boot 3.3.4、H2 MySQL mode |
| 命令 | `cd microservices && mvn -B test` |
| 测试类 | 4 |
| 测试总数 | 9 |
| 通过 | 9 |
| 失败 | 0 |
| 跳过 | 0 |
| 构建结论 | BUILD SUCCESS |

## 分服务结果

| 服务 | 测试类 | 数量 | 覆盖内容 | 结果 |
|---|---|---:|---|---|
| catalog-service | `CatalogApiAndE2ETest` | 3 | UC06 E2E、UC07 E2E、公开读写接口、生命周期规则 | 通过 |
| shop-service | `ShopApiAndE2ETest` | 2 | UC08 E2E、全部公开接口、目录不可用降级、装修规则 | 通过 |
| risk-service | `RiskApiAndE2ETest` | 2 | UC09 E2E、全部公开接口、违禁词规则、回调失败 Outbox | 通过 |
| behavior-service | `BehaviorApiAndE2ETest` | 2 | UC10 E2E、全部公开接口、搜索词规范化与校验 | 通过 |

## 五个用例 E2E 断言

1. UC06：按关键词、分类和价格筛选只返回匹配在售商品，详情数据正确。
2. UC07：草稿创建和编辑成功；提交后进入待审核；审核通过后上架；卖家可以下架。
3. UC08：访客可查看店铺；目录服务不可用时返回降级标志；卖家设置和装修持久化。
4. UC09：命中违禁词生成高风险审核单；管理员驳回后状态正确；目录回调失败写 Outbox。
5. UC10：浏览记录可新增、查询、删除和清空；搜索词被规范化；热词次数正确。

## 原始报告

Maven Surefire 在每次执行时生成 `microservices/*/target/surefire-reports/`。GitHub Actions 无论成功或失败都会上传这些目录为 `uc06-uc10-surefire-reports`，用于验收原始数据。Docker Desktop 在本次本地验证时未运行，因此本报告不声称完成了本地镜像运行或 Kubernetes 集群部署；对应配置由 CI 的 `kubectl kustomize` 步骤进行语法校验，集群运行证据需在可用集群中补录。
