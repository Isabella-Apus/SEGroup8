# UC15 Evidence

- `result-summary.json` records the 2026-08-27 `DOMAIN_C & UC15` Maven run: 9 tests passed with exit code 0.
- `raw-reports/surefire/` contains the original Unit, WebMvc, and Testcontainers MySQL 8.4.6 JUnit reports.
- `raw-reports/playwright/` contains the existing real-browser report and raw result files.
- `screenshots/` contains the refreshed buyer review page captured by Playwright.
- `logs/backend-domain-c.log` contains the current Domain-C test runner log.

The JUnit summary and the Playwright report were produced by separate runs. The historical Playwright PASS is retained as browser evidence and is not represented as part of the 2026-08-27 Maven execution.
