# 改造前后性能正式原始数据

本目录只保留课程要求的性能对比材料：

- `environment/`：同节点、同 MySQL、镜像/JAR 与资源配置；
- `performance/`：3 个接口，单体和微服务各 3 轮的逐请求 JSON、控制台摘要和资源采样。

旧二手单服务 HPA、过载探索和旧依赖故障子目录已移至仓库外 `selfwork`。最终统计、限制和最有代表性的 4 个结论见 `03_devops/cloud-native-experiments/README.md`。
