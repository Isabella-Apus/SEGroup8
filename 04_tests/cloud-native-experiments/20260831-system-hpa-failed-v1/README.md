# 完整系统 HPA 首轮调参失败证据

该轮已经观察到 `2 -> 4 -> 2`，但 `hpa-c5` 错误率为 5.1546%，超过脚本的 5%
门禁，因此退出码为 1，并自动删除临时 HPA、把后端恢复为运行前的 1 副本。

原因不是 HPA 对象未生效，而是首轮把二手列表设为 100 条/页，扩容期间大量逐条组装查询
导致超时。最终版本将负载改成真实页面使用的 20 条/页、保留 15 秒显式超时、把
`minReplicas` 固定为 2，并增加“新增 Pod Ready 后再次业务预热”的阶段。失败记录被保留，
没有用后续成功结果覆盖。
