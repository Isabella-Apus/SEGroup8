server.port: ${SERVER_PORT:8083}
spring:
  application.name: risk-service
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql:3306/risk_schema?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:risk}
    password: ${DB_PASSWORD:risk}
  sql.init.mode: always
management:
  endpoints.web.exposure.include: health,info
  endpoint.health.probes.enabled: true
clients.catalog-base-url: ${CATALOG_BASE_URL:http://catalog-service:8081}
