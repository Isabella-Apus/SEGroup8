apiVersion: apps/v1
kind: Deployment
metadata: {name: shop-service}
spec:
  replicas: 2
  selector: {matchLabels: {app: shop-service}}
  template:
    metadata: {labels: {app: shop-service}}
    spec:
      containers:
        - name: shop
          image: se-group8/shop-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports: [{containerPort: 8082}]
          env:
            - {name: DB_URL, value: "jdbc:mysql://mysql:3306/shop_schema?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai"}
            - {name: DB_USERNAME, value: shop}
            - name: DB_PASSWORD
              valueFrom: {secretKeyRef: {name: commerce-db-secret, key: shop-password}}
            - {name: CATALOG_BASE_URL, value: "http://catalog-service:8081"}
          startupProbe: {httpGet: {path: /actuator/health, port: 8082}, failureThreshold: 30, periodSeconds: 5}
          readinessProbe: {httpGet: {path: /actuator/health/readiness, port: 8082}, periodSeconds: 5}
          livenessProbe: {httpGet: {path: /actuator/health/liveness, port: 8082}, periodSeconds: 10}
          resources: {requests: {cpu: 100m, memory: 192Mi}, limits: {cpu: 500m, memory: 512Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: shop-service}
spec:
  selector: {app: shop-service}
  ports: [{port: 8082, targetPort: 8082}]
