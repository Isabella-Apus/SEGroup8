{{- if .Values.backend.autoscaling.enabled }}
{{- if lt (int .Values.backend.autoscaling.maxReplicas) (int .Values.backend.autoscaling.minReplicas) }}
{{- fail "backend.autoscaling.maxReplicas must be greater than or equal to minReplicas" }}
{{- end }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: segroup8-backend
  labels:
    {{- include "segroup8.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: segroup8-backend
  minReplicas: {{ .Values.backend.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.backend.autoscaling.maxReplicas }}
  behavior:
    scaleUp:
      stabilizationWindowSeconds: {{ .Values.backend.autoscaling.behavior.scaleUp.stabilizationWindowSeconds }}
      selectPolicy: Max
      policies:
        - type: Pods
          value: {{ .Values.backend.autoscaling.behavior.scaleUp.pods }}
          periodSeconds: {{ .Values.backend.autoscaling.behavior.scaleUp.periodSeconds }}
    scaleDown:
      stabilizationWindowSeconds: {{ .Values.backend.autoscaling.behavior.scaleDown.stabilizationWindowSeconds }}
      selectPolicy: Max
      policies:
        - type: Percent
          value: {{ .Values.backend.autoscaling.behavior.scaleDown.percent }}
          periodSeconds: {{ .Values.backend.autoscaling.behavior.scaleDown.periodSeconds }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.backend.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
