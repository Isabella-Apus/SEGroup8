apiVersion: v1
kind: Secret
metadata:
  name: segroup8-backend-secret
  namespace: segroup8
type: Opaque
stringData:
  DB_URL: "replace_me"
  DB_USERNAME: "replace_me"
  DB_PASSWORD: "replace_me"
  JWT_SECRET: "replace_with_at_least_32_random_bytes"
  RISK_AUDIT_LLM_API_KEY: "replace_me"
  REALTIME_ALLOWED_ORIGIN_PATTERNS: "https://replace.example"

